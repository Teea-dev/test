/* 
This is a shared file between the server and client Fetch API,
it stores and export the path to an endpoint.
it follows the convention E.g '/example' ---> _example
*/

const _faqs = '/faqs'

export {
    _faqs
}