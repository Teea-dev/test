'use client';
import React from 'react';
import { RouteLoadingSpinner } from './_global-components/LoadingSpinner';

function loading() {
    return (
        <RouteLoadingSpinner />
    )
}

export default loading