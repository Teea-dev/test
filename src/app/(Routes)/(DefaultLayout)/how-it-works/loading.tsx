import React from 'react';
import { RouteLoadingSpinner } from '@/app/_global-components/LoadingSpinner';

function loading() {
    return (
        <RouteLoadingSpinner />
    )
}

export default loading